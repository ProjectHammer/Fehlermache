import { count } from "console"
import { fsyncSync } from "fs"
import { Children, ReactElement } from "react"
import { createElementAccessChain, forEachChild, WatchDirectoryFlags } from "typescript"
import { projects } from "../shared/projects"
import Project from "../types/Project"
import ProjectProgress from "./ProjectProgress"
import ProjectRating from "./ProjectRating"
import ProjectTimes from "./ProjectTimes"

interface Props {
  project: Project
}

function ProjectRating(props: Props): ReactElement {

  const project = props.project
  
  //Project Book Rating Number Into An Array
  const totalItems = project.rating;
  const items = new Array(totalItems).fill(null);

  return (
      <p>Rating</p>
      <div>
        {items.map((_, idx) => <a className="a" key = {idx}>⭐</a>)}     
      </div>
  )
}

export default ProjectRating
